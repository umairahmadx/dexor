import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Dark palette.
  static const darkBg = Color(0xFF0D0D0F);
  static const darkSurface = Color(0xFF131317);
  static const darkCard = Color(0xFF18181D);
  static const darkBorder = Color(0xFF1E1E24);
  static const darkMuted = Color(0xFF6B7280);

  // Light palette.
  static const lightBg = Color(0xFFF5F5F7);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFF0F0F4);
  static const lightBorder = Color(0xFFE2E2E8);
  static const lightMuted = Color(0xFF9CA3AF);

  // Brand / Accent.
  static const cyan = Color(0xFF00E5CC);
  static const cyanDim = Color(0xFF00B8A3);
  static const red = Color(0xFFFF4D4D);
  static const green = Color(0xFF00D68F);
  static const amber = Color(0xFFFFB830);
  static const blue = Color(0xFF4D9FFF);

  // Semantic.
  static const valid = green;
  static const invalid = red;
  static const warning = amber;
}

